# 收口通知 · 2C-R5-FIX 三条偏离已裁定（场景冻结生效）

> **给代码实现方**：本通知不含新功能，**不需要改任何数值**。只有一处注释归属要落实。
> 依据：`spec/SCENE-LAYOUT-FIX-SPEC.md` **§10.9.1（设计方裁定）** 与 **§10.10（冻结已生效）**。

---

## 1. 结论：三条全部成立，设计方接受

| 你提出的 | 设计方复测（在盘代码） | 裁定 |
|---|---|---|
| ① `W5.guideGap` 阈值重立 ≥1.0m → **≥0.12m** | 车床包箱 max x = 11.19、导向线 x = 11.5 → **0.31m** | **接受** |
| ② `controls.minDistance = 6` 会钳制机位预设 | 8 预设半径 **6.05–41.23，无一 < 6**；cart 预设实测 lathe **543×342** / cart **224×205** | **接受** |
| ③ 导向线落 0.45m 空档（观察） | +x 现状 44×40px、距线 −1.30m；−x 备选 38×35px | **维持 +x 现状，不改，不新起轮次** |

**你的三处判断都是对的**，其中 ①② 的责任在设计方：

- ① 提示词 §3-③ 写"不变 (≥1.0m)"、§2-A 又把 0.32m 列为通过项 —— **提示词自相矛盾**。`≥1.0m` 是旧落位时代的数；你把它还原到它的**原则**（包箱不得压在导向线上）、余量取导向线自身带宽 0.12m（线宽，与落位无关），是正确做法。
- ② 我此前引用的 `latheH 315px` 对应 r<6 的**直算值，真实运行取不到**；`SCENE-LAYOUT-FIX-SPEC.md` §10.8 正文原先写的 "516 × 315" 也是错的（**测自旧的 `cart` 预设**却被标成 `lathe` 预设，且未走 `setView()`）。已改为 **501×333（lathe 预设）/ 543×342 与 224×205（cart 预设）**。

③ **维持 +x 端挂载**（不换 −x），理由：导向线是 2–3px 地面淡贴花（语义是作业区边界提示，不是结构物）；工具车停在导向线**外侧**（远离作业区通道）本来就对；−x 备选更小（38×35 < 44×40）且其屏幕框下沿正好贴上砂箱阵列剪影上沿，会引入新的贴靠风险。现状压阵列 0% —— **没有读感缺陷就不为对称性再动一次。**

## 2. 需要你做的一件事（仅注释）

把 `toolcart.js` / `viewport.js` 中关于**工具车挂 +x 端、导向线从两件之间穿过**的
"待设计方裁 / 待定"字样，改为：

```
§10.9.1 已裁定：维持 +x 端挂载，导向线落两件之间 0.45m 空档为几何必然，接受。
```

其余注释（§10.8-x 引用、"实现方判断（未走规格）"标注）已合规，不动。

## 3. 冻结生效 —— 请停止几何改动

`layout()` **61/61**、`cast_selftest` **45/45**、性能 36 draw call / 28,986 tri / 5 贴图（零增量）、
8 预设全可用（无钳制）、四机位截图 CONSOLE CLEAN —— 依据齐备，**§10.10 冻结自本通知起生效**。

- **此后任何布局 / 尺度改动必须新起 R6 并重走验收**，不得直接改数。
- 仍可自由改动的只有：**文案、材质参数（不改尺寸）、不影响几何的交互逻辑**。

## 4. 顺带固化两条规矩（此后一律执行）

1. **判据自指禁令**：`checks` 里每条 `threshold` 必须能回答"依据是什么"；**禁止"实测值 ±N"式的自指阈值**；数值改动必须与依据改动同时发生。
2. **机位预设钳制式**：任何机位预设必须满足 `|pos − target| ≥ minDistance + 0.05`；**近观读数的验收必须在 `setView()` 之后取**，不得用坐标直算。新增 / 调整预设时先查此式。

## 5. 不需要做的

- 不要动 `lathe.js` / `toolcart.js` 的任何数值（`POS` / `ROT_Y` / `SCALE` / `height` 全部保持）。
- 不要改 `layout()` 的任何阈值。
- 不要改 `HALL_VIEWS` 的 8 个预设。
- 不要为了让导向线"更干净"而挪工具车或改线的位置。

---

*场景已冻结，下一步是视频组按本版录制 + Step 3B `#/turn` / Step 4 `#/vault`（另出提示词，不含几何改动）。*
