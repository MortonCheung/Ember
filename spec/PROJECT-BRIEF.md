# 项目交接简报（给代码实现方）

> 你和设计方协作完成一个参赛作品。这份简报让你理解**在做什么、为什么这样做**，避免你按"通用官网"的直觉做错方向。

---

## 0. 交接物清单（先看这里）

本作品由**设计端**与**代码实现端**协作完成。你（代码实现端）会拿到这些东西：

| # | 交接物 | 位置 | 作用 |
|---|---|---|---|
| 0 | **交接总入口** | `README.md`（项目根） | **从这里开始** —— 阅读顺序、环境自检、交付要求 |
| 1 | 本文档 | `spec/PROJECT-BRIEF.md` | 理解在做什么、为什么 |
| 2 | 实现规格 | `spec/STEP1-IMPLEMENTATION-SPEC.md` | **开工依据** —— 验收标准、技术栈、Token、性能预算 |
| 3 | 场景清单 | `spec/SCENE-ASSETS-STEP2.md` | Step 2 才用，先别看 |
| 3b | **道具细节补强（当前进行中）** | `spec/SCENE-ASSETS-STEP2B-DETAIL.md` | **下一轮开工依据** —— 从"尺寸对"到"像"，含剪影测试判据与 D1–D5 验收标准 |
| 3c | **交互与评分规则（创新性 40 分的核心）** | `spec/STEP3-INTERACTION-SCORING-SPEC.md` | 浇铸 / 车削的参数→扣分规则、权重、边界情况、T1–T8 验收标准 |
| 3d | **场景布局与尺度修正（当前进行中，Step 2C）** | `spec/SCENE-LAYOUT-FIX-SPEC.md` | **下一轮开工依据** —— 冲天炉 ×1.35 / 车床按真实尺寸重建＋换机位 / 砂箱落位 / 7 处悬空构件处置；含 V1–V8 验收 |
| 4 | 设计稿导出图 | `design/`（7 张 PNG + README 说明） | **视觉验收基准** —— 不需要访问 Ardot |
| 5 | 可运行 demo | `demo/` | 基线参考 —— 已跑通，`启动预览.bat` 双击可看 |
| 6 | 截图工具 | `tools/screenshot.py` | 无头截图，验收自测用 |

**视觉源文件（Ardot 画布，可选）**：
🔗 https://ardot.tencent.com/file/726075513358995

> 通常不需要访问画布 —— `design/` 里已有全部 7 屏的导出图。
> **冲突时的优先级**：实现规格 > 设计稿导出图 > 本文档 > demo 代码。

---

## 1. 这是什么

**2026 年第五届辽宁省大学生虚拟现实与新媒体创新设计大赛**
- 赛道：虚拟现实设计
- 方向：文化创意（文化+科技融合、艺术+科技融合）
- 主题：以「艺术与科技」为核心，突出艺术创新、数字创意、数字文旅、中国传统文化、地域文化

**作品**：中国工业博物馆（沈阳）的 Web 数字展馆。

---

## 2. 这个作品的立意（很重要，别做成普通官网）

**「炉火不灭」**

一句话：**把一座已被封存的车间，重启为一台可以对谈的机器。**

中国工业博物馆的铸造馆，是原沈阳铸造厂的翻砂车间原址。里面那台十吨冲天炉，1957 年投用、2007 年熄火，熔化过近百万吨铁水，生产铸件 60 余万吨，现在是全国重点文物保护单位里一个静止的"工业琥珀"。

我们做的事：**让它重新点火，但点火的人是观众。**

所以这不是一个"信息展示网站"，是一个**参与式叙事**。四层体验递进：

| 层 | 名称 | 用户做什么 |
|---|---|---|
| 01 | 看见 · 云端铁西 | 认识铁西"东方鲁尔"的分量 |
| 02 | 看懂 · 铁流凝变 | 理解这些机器为什么重要 |
| 03 | 参与 · 车间劳作 | **亲手浇铸、亲手车削**（创意峰值） |
| 04 | 带走 · 炉火不灭 | 生成专属数字工牌 |

**第 03 层是整个作品的灵魂。** 不是"点开看视频"，而是"你操作参数 → 铸件质量因此改变 → 你被评分"。

---

## 3. 评分标准（决定技术取舍）

| category | 分值 | 我们的应对 |
|---|---|---|
| 创新性 | **40** | 参与式叙事 + 手势/参数驱动模拟（不是播放视频） |
| 可行性 | 20 | 全开源栈、无付费依赖、含降级路径 |
| 设计表达 | 20 | 完整设计系统（Token 化），视觉统一 |
| 技术应用 | 20 | 真实 WebGL 实时渲染 + WebXR，非常规模板站 |

**创新性 40 分比"可行性+设计表达"加起来还重。** 所以：**宁可技术保守，也要创意锋利。** 不要为了炫技引入不可控的复杂度。

---

## 4. 真实素材（可以直接用的史实，增加可信度）

写文案、做场景时用这些**真数据**，不要编：

**序厅雕塑《铁流凝变》**
- 长 22 米、高 11.5 米，凝固新中国第一炉钢水倾泻的瞬间

**新中国第一枚金属国徽**
- 直径 2.4 米、重 487 公斤
- 焦百顺团队在沈阳第一机器厂（今沈阳第一机床厂）铸造

**C620-1 普通车床**
- 1955 年沈阳第一机床厂研制
- 图案登上第三套人民币 2 元纸币正面
- 年产量最高 2200 台，国内市场占有率超八成，远销 70 多个国家

**铸造馆（原沈阳铸造厂翻砂车间）**
- 2018 年入选第二批国家工业遗产
- 十吨冲天炉：炉身高 12 米、总重 300 吨，1957 投用–2007 熄火
- 熔化近百万吨铁水，生产铸件 60 余万吨
- 空间：举架 30 米、纵深 200 米
- 博物馆占地 5.3 万 m²，藏品 1.5–3 万余件

**劳模故事（Step 3 用）**
- 张成哲（沈阳重型机器厂钳工，17 岁进厂，800+ 技术革新，18 项填补国内空白）
- 吴家柱（沈阳气体压缩机厂，腾出自家住房做技术学习场所）
- 赵国友（沈阳第三机器厂车工，塔轮加工 4 小时 → 50 分钟）
- 孟泰（鞍钢，"孟泰仓库"）

**文化彩蛋**
- 2024 年央视龙年春晚沈阳分会场设在中国工业博物馆铸造馆
- 使用 8 台新松 SR210D 机械臂；齿轮道具内置可敲击军鼓；舞美以工字钢为灵感

**沈阳铁西背景**
- "东方鲁尔"
- 一五时期聚集全国十分之一以上工业产能
- 156 项国家重点工程中 24 项落户辽宁

---

## 5. 我们要解决的"真实痛点"（可行性论证的抓手）

这不是拍脑袋的点子。公开报道显示：

- **光明日报（2026-05）**：调研指出博物馆展陈仍以展板 + 老照片为主，代入感弱、缺互动、忽视年轻观众
- **沈阳日报（2022）**：改造顾问潜伟专访，提出「活态化 + 动化线 + 符号化 + 视觉化 + 数字化 + 体验化」六化思路，以及「去除 + 保留 + 恢复 + 修改 + 新增」五路径

**我们的方案正好落在"数字化 + 体验化"这条官方论证过的缺口上。** 这就是可行性的证据。

---

## 6. 视觉基调

**深色工业风。** 记忆点只有一个词：**铁水橙**。

- 底色近黑（`#0E1116`），像熄火后的车间
- 唯一强调色是铁水的橙红（`#E8663C`），像炉口的光
- 其余全部是钢灰、暗铁、冷白

**橙色是稀缺资源。** 只用在：主按钮、当前选中态、关键数值、炉火光源。
铺开用就废了。

---

## 7. 协作规则

1. **设计方管"长什么样、怎么交互"，你管"怎么实现"**
2. **Token 不要改**（色彩/间距/字型），那是设计系统
3. **范围不要扩**，按 Step 1 → 2 → 3 走，每步验收后再往下
4. **有问题先问**，不要自己发挥——你做的每个设计决策都可能和另一屏冲突

---

## 8. 当前阶段

**Step 1：空壳跑通。**
目标不是好看的成品，是**验证基础管线能跑**。详见 `STEP1-IMPLEMENTATION-SPEC.md`。

上次同类项目失败的原因就是**跳过 Step 1 直接上全量**，结果建模精度差、错误频发、无法交付。这次我们反过来：**先把地基浇好。**

---

## 9. 本地环境说明（接手前必读）

### 9.1 一键启动

demo 目录下有 **`启动预览.bat`**，**双击即可**。它会自动：检查 Node.js → 安装依赖 → 构建 → 启动服务 → 打开浏览器。

如果双击闪退，说明没装 Node.js：去 https://nodejs.org 下载 LTS 版安装，然后重开窗口。

### 9.2 手工启动

```bash
cd /d "D:\agent projects\project\demo"
npm install
npm run build
npm run preview
```

访问 `http://localhost:4173/`（展厅页 `http://localhost:4173/#/hall`）。
关闭服务：在窗口按 `Ctrl + C`，再按 `Y`。

### 9.3 ⚠️ 关于 shell 的坑（仅影响 AI 代理，不影响你自己的终端）

**先说结论：你在自己的 cmd / PowerShell 7 里敲命令，一切正常，随便用管道。**
这一节只对"AI 代理替我执行命令"的场景有效。

**原因**：WorkBuddy 内置了一个**精简过的 PortableGit** 作为 AI 的命令执行环境，位置在
`C:\Users\savetime\.workbuddy\binaries\PortableGit\versions\1.2.0\bin\`，
里面**只有一个文件：`bash.exe`**。正常 Git for Windows 该目录下有 150 多个 GNU 命令，
这个版本把 `tail`、`ls`、`dirname`、`sed`、`awk` 全部剥掉了。

后果：**AI 执行的带管道命令会假报失败**。例如：

```bash
npm install 2>&1 | tail -20     # ❌ AI 执行时显示 failed，但其实 npm 已经装完了
```

失败的是 `tail`（不存在），不是 `npm`。管道右侧一崩，整个命令退出码就是非 0。

**如果你在让 AI 帮你执行命令**，请这样规避：
1. 告诉它**不要用管道 `|`**，直接跑命令看原始输出
2. 需要列目录 / 操作文件 → 让它走 PowerShell，不要走 bash
3. 判断是否成功 → **看命令自己的输出**（如 npm 打印 `added N packages`），别只看退出码

**如果你自己敲**：无需任何规避，正常用。

### 9.4 无头截图（需要时）

环境里有 Edge（`C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe`）。
注意：Edge 的 `--headless --screenshot=xxx.png` 参数**在本机静默失败**（返回码 0 但不产文件），必须走 CDP（Chrome DevTools Protocol）。

参考实现：`.workbuddy/cdp_shot.py`（纯标准库，手写 WebSocket 客户端 + `Page.captureScreenshot`）。

---

*有任何不确定的地方，回到规格文档查；规格没写的，问设计方。*
